# 🧠 پروژه تحلیل احساسات فارسی با Fine-tuning مدل BERT

این پروژه یک نمونه کامل از فرآیند Fine-tuning یک مدل ترنسفورمر (HooshvareLab/bert-fa-base-uncased) برای تحلیل احساسات نظرات کاربران به زبان فارسی است.

**دموی زنده پروژه:**
*(در صورت فعال بودن، لینک Gradio خود را اینجا قرار دهید)*
> **توجه:** لینک‌های دموی Gradio پس از ۷۲ ساعت منقضی می‌شوند.

---

## 🎯 هدف پروژه

هدف، ساخت یک طبقه‌بند (Classifier) بود که بتواند نظرات متنی کاربران را به دو دسته "مثبت" (Happy) و "منفی" (Sad) تقسیم کند.

## 🛠️ تکنولوژی‌ها و کتابخانه‌ها

- Python 3
- PyTorch
- **Hugging Face Transformers**: برای مدل‌سازی و فرآیند Fine-tuning
- **Hugging Face Datasets**: برای مدیریت داده‌ها
- **Hugging Face Evaluate**: برای ارزیابی مدل
- **Gradio**: برای ساخت رابط کاربری و دموی تعاملی
- **Google Colab**: برای محیط توسعه و دسترسی به GPU

## 📈 فرآیند کار

1.  **آماده‌سازی داده:** یک دیتاست نمونه از نظرات کاربران به صورت دستی ایجاد شد.
2.  **توکنایز کردن:** با استفاده از توکنایزر `bert-fa-base-uncased`، متن‌ها به فرمت قابل فهم برای مدل تبدیل شدند.
3.  **Fine-tuning:** مدل پایه روی دیتاست سفارشی برای ۳ Epoch آموزش داده شد. از کلاس `Trainer` و `DataCollatorWithPadding` برای مدیریت بهینه آموزش استفاده شد.
4.  **ارزیابی:** مدل نهایی روی چند نمونه تست ارزیابی شد.

## 📊 نتایج و تحلیل

مدل با موفقیت توانست احساسات جملات ساده را تشخیص دهد. به عنوان مثال:
- **جمله مثبت:** "این بهترین پیتزایی بود که در تمام عمرم خوردم!" -> **نتیجه: مثبت (LABEL_1)**
- **جمله منفی:** "غذا یخ کرده بود و پیک هم نیم ساعت تاخیر داشت. افتضاح!" -> **نتیجه: منفی (LABEL_0)**

**محدودیت‌ها:**
به دلیل استفاده از یک دیتاست آموزشی بسیار کوچک (۱۲ نمونه)، امتیاز اطمینان مدل (score) پایین و نزدیک به ۰.۵ بود. این نشان می‌دهد که مدل در پیش‌بینی‌های خود دچار عدم قطعیت است.

**راه‌های بهبود:**
برای یک پروژه واقعی، قدم بعدی استفاده از دیتاست کامل (با هزاران نمونه) و آموزش برای مدت طولانی‌تر خواهد بود تا دقت و اطمینان مدل به سطح قابل قبولی برسد.

## 🚀 نحوه اجرا

کافیست نوت‌بوک `Persian_Sentiment_Analysis_Finetuning.ipynb` را در محیطی مانند Google Colab باز کرده و تمام سلول‌ها را به ترتیب اجرا نمایید.
